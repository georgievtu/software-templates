package state.strategy.decorator;

public class VisualNavigation implements NavigationStrategy {
    @Override
    public double navigate(double distance) {
        // TODO Visual strategy can fly at 60km/h
        // TODO Return the flight time
        // TODO time = distance / speed
        return 0;
    }
}
